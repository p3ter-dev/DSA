#include <iostream>

using namespace std;

class HashMap {
public:
    int size;
    int keyMap;
    HashMap(int size=53) {
        this->keyMap = new Array(size);
    }

    void hash(key) {
        int total = 0;
        int PRIME_NUMBER = 31;
        for (int i = 0; min(key.length, 100); i++) {
            char character = key[i];
            int value = character.length()
        }
    }
}