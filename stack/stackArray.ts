// let stack = []

// stack.push("google")
// stack.push("instagram")
// stack.push("youtube")

// stack.unshift("google")
// stack.unshift("instagram")
// stack.unshift("youtube")
// in arrays pushing and poping are the best methods in implementing stacks
// because there will not be shifting the index of the arrays but in shifting and unshifting
// there will be shifting the index which is not suitable for a large datasets.